DJANGO_DEBUG=true
DJANGO_SECRET_KEY=replace-this-in-production
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
# Explicit dev origins (also added as defaults in settings.py):
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173
# Leave empty for SQLite (default). Use PostgreSQL in deployment:
# DATABASE_URL=postgresql://geobharat:password@localhost:5432/geobharat
